# ESP32 DevKit V1 project that blinks an LED connected to GPIO 2 every 1 second using non-blocking timing. The code should use Arduino framework, avoid delay(), and be memory efficient.

## Overview
ESP32 DevKit V1 project that blinks an LED connected to GPIO 2 every 1 second using non-blocking timing. The code should use Arduino framework, avoid delay(), and be memory efficient.

## Notes
This documentation was auto-generated by the MCP Documentation Generator.
